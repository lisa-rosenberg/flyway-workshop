# Task 1: Add `phone_number` column

## Goal
Extend `user_profile` table with a phone number.

## Task
- Create a new versioned Flyway migration
- Add a column `phone_number` to the `user_profile` table
- The column must be nullable
- Think about a reasonable data type and length for a phone number
- Ensure that the migration can be applied