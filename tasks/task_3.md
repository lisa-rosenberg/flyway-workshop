# Task 3: Insert additional test data

## Goal
Add more test data to the `user_profile` table.

## Task
- Create a new versioned Flyway migration.
- At least:
  - use different `status` values
  - leave some `phone_number` values `NULL`
- Ensure that the migration can be applied.